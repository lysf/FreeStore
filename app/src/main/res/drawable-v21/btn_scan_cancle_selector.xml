<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/translucent_15_black">
    <item android:id="@android:id/mask"  android:drawable="@drawable/detail_progress_pause_bg">
    </item>
    <item>
        <shape>
            <solid android:color="@android:color/transparent"/>
            <stroke android:color="@color/personal_dialog_line"
                android:width="1px"/>
            <!-- 圆角 -->
            <corners
                android:radius="@dimen/common_btn_corners"/>
        </shape>
    </item>
</ripple>
